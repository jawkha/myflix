/**
 * The following tests will have to be written:
 * Small navbar displayed for small screens and large for large ones
 * Links work correctly
 * Search works as expected, i.e. as follows:
 * 1. input field is autofocussed on load
 * 2. not initiated when the input field is empty
 * 3. successfully fetches data when form is submitted
 * 4. input field is emptied once the search is complete
 * 5. no hover effect on touch devices
 */
